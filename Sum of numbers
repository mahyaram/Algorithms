int sum(int n) 
{ 
    int result = 0; // firstly we need to initialize result 
  
    // One by one compute sum of digits in every number from 
    // 1 to n 
    for (int x = 1; x <= n; x++) 
        result += sum(x); 
  
    return result; 
}
